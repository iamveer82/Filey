export const metadata = {
  title: 'Get started · Filey',
  description: 'Set up your Filey account in under 2 minutes — profile, UAE TRN, AI provider.',
  robots: { index: false, follow: false },
};

export default function OnboardingLayout({ children }) {
  return children;
}
