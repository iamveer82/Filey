/**
 * Filey composite patterns — barrel export.
 *
 * Patterns combine multiple primitives into reusable page-level building
 * blocks. Prefer these over hand-rolling the same shape on every page.
 */

export { StatCard } from './StatCard';
export { PageHeader, SectionHead } from './PageHeader';
export { FormField } from './FormField';
export { KeyValueGrid } from './KeyValueGrid';
